<?xml version="1.0"?>
<clickhouse>
    <!-- Railway's private network is IPv6. Without this, other services cannot
         reach ClickHouse at <service>.railway.internal. -->
    <listen_host>::</listen_host>

    <!-- Upstream's single-node tuning: disable internal telemetry logs that add
         storage and I/O overhead but are not useful on one node. Kept verbatim
         from the project's clickhouse-server-config.xml. -->
    <trace_log remove="1"/>
    <text_log remove="1"/>
    <part_log remove="1"/>
    <metric_log remove="1"/>
    <asynchronous_metric_log remove="1"/>
    <background_schedule_pool_log remove="1"/>
    <query_metric_log remove="1"/>
    <query_thread_log remove="1"/>
    <query_views_log remove="1"/>
    <session_log remove="1"/>
    <crash_log remove="1"/>
    <opentelemetry_span_log remove="1"/>
    <zookeeper_log remove="1"/>
    <blob_storage_log remove="1"/>
    <processors_profile_log remove="1"/>
    <asynchronous_insert_log remove="1"/>

    <!-- Keep operator-facing logs for troubleshooting, but bound their disk use. -->
    <query_log>
        <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
    </query_log>
    <error_log>
        <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
    </error_log>
</clickhouse>
